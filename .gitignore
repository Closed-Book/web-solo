__pycache__/
*.pyc

# 持久化 profile 目录：里面是真 cookie 和会话，永远不要提交
.web-solo-profiles/
web-solo-profiles/
profiles/
*-profile/
*.profile/

# 加速层 OpenCLI 装在本目录时的产物（setup.sh 用 npm i --prefix 装，三样都是它生成的）
node_modules/
/package.json
/package-lock.json

# README / SKILL 示例命令的输出产物
/a.png
/out/
